# Keyword Cluster Example

Input:
"Cluster keywords around influencer analytics."

Expected output shape:

- primary pillar topic
- supporting cluster pages
- intent grouping
- internal-link suggestions

Good answer qualities:

- cluster structure reflects how content should actually be published
- overlap and cannibalization are reduced
