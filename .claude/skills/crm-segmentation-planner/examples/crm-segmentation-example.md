# CRM Segmentation Example

Input:
"Segment trial users for activation emails."

Expected output shape:

- key trial-user segments
- logic for each segment
- message objective by segment
- trigger events and KPIs

Good answer qualities:

- segments are actionable
- each segment supports a distinct campaign
